# 📸 Socialy - תיעוד הפרויקט

## 🎯 מה זה הפרויקט?

זוהי אפליקציית Socialy הבנויה עם טכנולוגיות מודרניות:
- **Next.js** - פריימוורק React לבניית אתרים
- **TypeScript** - JavaScript עם טייפים
- **Prisma** - ORM לעבודה עם דאטאבייס
- **Tailwind CSS** - סטיילינג מהיר עם classes
- **SQLite/PostgreSQL** - דאטאבייס

---

## 📁 מבנה התיקיות

```
Socialy/
├── prisma/                    # 🗄️ דאטאבייס
│   ├── schema.prisma          # הגדרת הטבלאות
│   ├── seed.ts                # דאטא התחלתי
│   └── dev.db                 # קובץ הדאטאבייס (SQLite)
│
├── src/                       # 📦 קוד המקור
│   ├── components/            # 🧩 קומפוננטות React
│   │   ├── layout/
│   │   │   └── BottomNav.tsx  # תפריט תחתון
│   │   ├── profile/
│   │   │   ├── PostsGrid.tsx  # גריד פוסטים בפרופיל
│   │   │   └── StoryHighlight.tsx
│   │   └── shared/
│   │       ├── Avatar.tsx     # תמונת פרופיל עגולה
│   │       └── Button.tsx     # כפתור כללי
│   │
│   ├── context/
│   │   └── AuthContext.tsx    # ניהול התחברות משתמש
│   │
│   ├── hooks/
│   │   └── useApi.ts          # הוק לקריאות API
│   │
│   ├── lib/
│   │   ├── auth.ts            # פונקציות אימות
│   │   └── prisma.ts          # חיבור לדאטאבייס
│   │
│   ├── pages/                 # 📄 דפים (כל קובץ = נתיב)
│   │   ├── _app.tsx           # עטיפה כללית לאפליקציה
│   │   ├── _document.tsx      # HTML בסיסי
│   │   ├── index.tsx          # 🏠 דף הבית (פיד)
│   │   ├── login.tsx          # 🔐 דף התחברות
│   │   ├── profile.tsx        # 👤 דף פרופיל
│   │   ├── create.tsx         # ➕ יצירת פוסט
│   │   ├── story.tsx          # 📖 צפייה בסטורי
│   │   ├── messages.tsx       # 💬 רשימת הודעות
│   │   ├── chat/[userId].tsx  # 💬 צ'אט עם משתמש
│   │   ├── comments.tsx       # 💭 תגובות לפוסט
│   │   ├── reels.tsx          # 🎬 רילס
│   │   ├── settings.tsx       # ⚙️ הגדרות
│   │   │
│   │   └── api/               # 🔌 API Routes (שרת)
│   │       ├── auth/
│   │       │   ├── login.ts   # התחברות
│   │       │   ├── register.ts # הרשמה
│   │       │   └── me.ts      # מידע על המשתמש הנוכחי
│   │       ├── posts/
│   │       │   ├── index.ts   # רשימת פוסטים / יצירה
│   │       │   └── [id]/
│   │       │       ├── like.ts     # לייק לפוסט
│   │       │       └── comments.ts # תגובות לפוסט
│   │       ├── stories/
│   │       │   └── index.ts   # סטוריז
│   │       ├── messages/
│   │       │   ├── index.ts   # כל ההודעות
│   │       │   └── [userId].ts # הודעות עם משתמש ספציפי
│   │       └── users/
│   │           ├── index.ts   # חיפוש משתמשים
│   │           └── [username].ts # פרופיל משתמש
│   │
│   └── styles/
│       └── globals.css        # 🎨 סטיילים גלובליים
│
├── package.json               # 📦 הגדרות פרויקט ותלויות
├── tailwind.config.ts         # 🎨 הגדרות Tailwind
├── tsconfig.json              # ⚙️ הגדרות TypeScript
└── .env                       # 🔐 משתני סביבה (סודי!)
```

---

## 🔧 פקודות חשובות

### התקנה והרצה

| פקודה | מה עושה |
|-------|---------|
| `npm install` | התקנת כל החבילות |
| `npm run dev` | הרצת האפליקציה במצב פיתוח (localhost:3000) |
| `npm run build` | בניית האפליקציה לפרודקשן |
| `npm start` | הרצה במצב פרודקשן |

### דאטאבייס (Prisma)

| פקודה | מה עושה |
|-------|---------|
| `npx prisma db push` | יצירת/עדכון הטבלאות בדאטאבייס |
| `npm run db:seed` | מילוי דאטא התחלתי (משתמשים, פוסטים...) |
| `npx prisma studio` | ממשק גרפי לצפייה בדאטאבייס |
| `npx prisma generate` | יצירת הקליינט של Prisma |
| `npm run db:reset` | מחיקת הכל ויצירה מחדש |

---

## 📄 קבצים חשובים - הסבר מפורט

### 1. `prisma/schema.prisma` - הגדרת הדאטאבייס

```prisma
// הגדרת חיבור לדאטאבייס
datasource db {
  provider = "sqlite"  // או "postgresql" לענן
  url      = env("DATABASE_URL")
}

// דוגמה לטבלה
model User {
  id        String   @id @default(cuid())  // מזהה ייחודי
  username  String   @unique               // שם משתמש (ייחודי)
  password  String                         // סיסמה מוצפנת
  posts     Post[]                         // קשר לפוסטים
}
```

### 2. `src/pages/_app.tsx` - עטיפת האפליקציה

קובץ זה עוטף את כל הדפים ומוסיף:
- בדיקת התחברות
- תפריט תחתון (BottomNav)
- סטיילים גלובליים

### 3. `src/hooks/useApi.ts` - קריאות לשרת

```typescript
const { get, post } = useApi();

// קריאת דאטא
const posts = await get('/api/posts');

// שליחת דאטא
await post('/api/posts', { image: '...', caption: '...' });
```

### 4. `src/lib/prisma.ts` - חיבור לדאטאבייס

```typescript
import prisma from '@/lib/prisma';

// שימוש ב-API routes
const users = await prisma.user.findMany();
const post = await prisma.post.create({ data: {...} });
```

### 5. `.env` - משתני סביבה

```env
# חיבור לדאטאבייס
DATABASE_URL="file:./dev.db"  # SQLite מקומי

# או לענן:
DATABASE_URL="postgresql://user:pass@host/db"
```

⚠️ **חשוב:** קובץ זה לא נשמר ב-Git כי הוא מכיל סיסמאות!

---

## 🔌 API Routes - נקודות קצה

### אימות (Auth)
| Method | Endpoint | תיאור |
|--------|----------|-------|
| POST | `/api/auth/login` | התחברות |
| POST | `/api/auth/register` | הרשמה |
| GET | `/api/auth/me` | מידע על המשתמש הנוכחי |

### פוסטים
| Method | Endpoint | תיאור |
|--------|----------|-------|
| GET | `/api/posts` | כל הפוסטים |
| POST | `/api/posts` | יצירת פוסט חדש |
| POST | `/api/posts/[id]/like` | לייק/אנלייק |
| GET | `/api/posts/[id]/comments` | תגובות לפוסט |
| POST | `/api/posts/[id]/comments` | הוספת תגובה |

### סטוריז
| Method | Endpoint | תיאור |
|--------|----------|-------|
| GET | `/api/stories` | כל הסטוריז |
| POST | `/api/stories` | יצירת סטורי |

### הודעות
| Method | Endpoint | תיאור |
|--------|----------|-------|
| GET | `/api/messages` | כל השיחות |
| GET | `/api/messages/[userId]` | הודעות עם משתמש |
| POST | `/api/messages/[userId]` | שליחת הודעה |

### משתמשים
| Method | Endpoint | תיאור |
|--------|----------|-------|
| GET | `/api/users` | חיפוש משתמשים |
| GET | `/api/users/[username]` | פרופיל משתמש |

---

## 🗄️ מודלים בדאטאבייס

| מודל | תיאור | שדות עיקריים |
|------|-------|--------------|
| **User** | משתמש | username, password, avatar, bio |
| **Post** | פוסט | image, caption, userId |
| **Comment** | תגובה | text, userId, postId |
| **Like** | לייק | userId, postId |
| **Story** | סטורי | image, userId, expiresAt |
| **Message** | הודעה | text, senderId, receiverId |
| **Follow** | עוקב | followerId, followingId |
| **Highlight** | היילייט | name, image, userId |

---

## 🚀 איך להתחיל?

### פעם ראשונה:
```bash
# 1. התקנת חבילות
npm install

# 2. יצירת הדאטאבייס
npx prisma db push

# 3. מילוי דאטא התחלתי
npm run db:seed

# 4. הרצת האפליקציה
npm run dev
```

### התחברות:
- **Username:** `1`
- **Password:** `1`

---

## ☁️ העלאה לענן

### שלב 1: דאטאבייס בענן
1. צרי חשבון ב-[Neon](https://neon.tech) או [Supabase](https://supabase.com)
2. צרי פרויקט חדש
3. העתיקי את ה-connection string

### שלב 2: עדכון הפרויקט
1. שני את `prisma/schema.prisma`:
   ```prisma
   provider = "postgresql"
   ```
2. צרי קובץ `.env` עם ה-DATABASE_URL
3. הריצי:
   ```bash
   npx prisma db push
   npm run db:seed
   ```

### שלב 3: העלאה ל-Vercel
1. העלי את הקוד ל-GitHub
2. חברי את ה-repo ל-Vercel
3. הוסיפי את ה-DATABASE_URL ב-Environment Variables

---

## 🛠️ טיפים לפיתוח

1. **Prisma Studio** - לראות את הדאטא:
   ```bash
   npx prisma studio
   ```

2. **Hot Reload** - שינויים בקוד מתעדכנים אוטומטית

3. **TypeScript** - שגיאות יופיעו בזמן כתיבה

4. **Tailwind IntelliSense** - התקיני את התוסף ב-VSCode

---

## 📱 פיצ'רים באפליקציה

- ✅ התחברות והרשמה
- ✅ פיד פוסטים
- ✅ לייקים ותגובות
- ✅ סטוריז עם טיימר
- ✅ יצירת פוסטים
- ✅ הודעות פרטיות
- ✅ פרופיל משתמש
- ✅ עיצוב מותאם למובייל

---

**נבנה עם ❤️ באמצעות Next.js, Prisma, ו-Tailwind CSS**


