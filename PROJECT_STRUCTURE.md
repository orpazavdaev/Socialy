# Instagram Clone - Next.js Project

## 📱 תיאור הפרויקט

פרויקט זה הוא שכפול עיצובי של אפליקציית אינסטגרם, בנוי ב-Next.js 14 עם App Router.
הפרויקט מותאם למובייל ומתמקד בעיצוב לפי Figma.

## 🎨 טכנולוגיות

- **Next.js 14** - App Router
- **TypeScript** - Type safety
- **Tailwind CSS** - Styling
- **Lucide React** - Icons

## 📁 מבנה הקבצים

```
instagram/
├── src/
│   ├── app/
│   │   ├── layout.tsx              # Layout ראשי
│   │   ├── page.tsx                # דף הבית
│   │   ├── globals.css             # סגנונות גלובליים
│   │   ├── api/                    # API Routes
│   │   │   ├── route.ts            # Main API endpoint
│   │   │   ├── users/
│   │   │   │   └── route.ts        # Users API
│   │   │   └── posts/
│   │   │       └── route.ts        # Posts API
│   │   ├── profile/
│   │   │   └── page.tsx            # דף פרופיל
│   │   ├── comments/
│   │   │   └── page.tsx            # דף תגובות
│   │   ├── settings/
│   │   │   └── page.tsx            # הגדרות
│   │   ├── story/
│   │   │   └── page.tsx            # צפייה בסטורי
│   │   ├── reels/
│   │   │   └── page.tsx            # רילס / חיפוש
│   │   ├── create/
│   │   │   └── page.tsx            # יצירת פוסט
│   │   └── search/
│   │       └── page.tsx            # חיפוש
│   └── components/
│       ├── layout/
│       │   └── BottomNav.tsx       # ניווט תחתון
│       ├── profile/
│       │   ├── StoryHighlight.tsx  # היילייט סטורי
│       │   └── PostsGrid.tsx       # גריד תמונות
│       └── shared/
│           ├── Avatar.tsx          # אווטאר משתמש
│           └── Button.tsx          # כפתור
├── public/
├── package.json
├── tailwind.config.ts
├── tsconfig.json
└── next.config.js
```

## 📄 דפים

### 1. פרופיל - `/profile`
- תמונת פרופיל עם סטטיסטיקות (פוסטים, עוקבים, עוקב אחרי)
- ביוגרפיה
- היילייטס של סטוריז (New, Me, Food, Life, Sea)
- כפתורי Edit Profile ו-Share Profile
- טאבים: Grid / Reels / Tagged
- גריד תמונות 3x3

### 2. תגובות - `/comments`
- כרטיס פוסט עם הזכרות
- רשימת תגובות עם אווטארים
- לייקים ותשובות לכל תגובה
- זמן פרסום

### 3. הגדרות - `/settings`
- Meta Accounts Center
- How to use Instagram
- Who can see your content
- How others can interact with you

### 4. סטורי - `/story`
- תצוגת סטורי מלא מסך (רקע כהה)
- פס התקדמות
- פרטי משתמש ומוזיקה
- כפתורי X, More, Volume
- שדה תגובה

### 5. רילס - `/reels`
- שורת חיפוש
- גריד תמונות 3x3

### 6. יצירת פוסט - `/create`
- Header עם X, "New post", Next
- תצוגת תמונה נבחרת
- גלריה עם Camera button
- טאבים: POST / STORY / REEL

## 🎯 עיצוב (Light Theme)

### צבעים
- **רקע ראשי**: `#FFFFFF`
- **רקע משני**: `#FAFAFA`
- **גבולות**: `#DBDBDB`
- **טקסט ראשי**: `#262626`
- **טקסט משני**: `#8E8E8E`
- **אקסנט (כחול)**: `#0095F6`
- **גרדיאנט סטורי**: `linear-gradient(45deg, #f09433, #e6683c, #dc2743, #cc2366, #bc1888)`

### טיפוגרפיה
- **פונט**: System UI / SF Pro
- **כותרות**: 600-700 weight
- **גוף טקסט**: 400-500 weight

### ניווט תחתון (Bottom Nav Pill)
- רקע כהה מעוגל
- אייקונים: Home, Search, Create, Reels, Profile

## 🚀 התקנה והרצה

```bash
# התקנת dependencies
npm install

# הרצה בפיתוח
npm run dev

# בנייה לפרודקשן
npm run build
```

## 📡 API Routes

הפרויקט כולל API Routes מוכנים להרחבה:

- `GET /api` - מידע על ה-API
- `GET /api/users` - רשימת משתמשים
- `POST /api/users` - יצירת משתמש
- `GET /api/posts` - רשימת פוסטים
- `POST /api/posts` - יצירת פוסט
